const config = {
    verbose: true,
    preset: 'ts-jest',
    testEnvironment: 'node',
    // Qui puoi aggiungere altre configurazioni Jest
};

module.exports = config; // Nota: stiamo usando module.exports invece di "export default config"
