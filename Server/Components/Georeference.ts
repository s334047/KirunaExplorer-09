class Area{
   id: number
   name: string
   vertex: Number[][];

    constructor(id: number, name: string, vertex: number[][]){
        this.id = id,
        this.name = name,
        this.vertex = vertex;        
    }
}

export {Area};