class Area{
   id: number
   name: string
   vertex: string

    constructor(id: number, name: string, vertex: string){
        this.id = id,
        this.name = name,
        this.vertex = vertex;        
    }
}

export {Area};